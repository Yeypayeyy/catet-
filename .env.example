# Supabase → Settings → Database → Connection string → Transaction pooler
# WAJIB port 6543 (pooler), BUKAN 5432 (direct connection).
DATABASE_URL=

# Supabase → Settings → API
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_ANON_KEY=
